# TODO

## Bun runtime/toolchain migration (explicit architecture decision, supersedes Node+npm default)

Scope: `restful-api/`, `mcp-server-v2/`, and the separate writable repo
`~/Projekt/meshcore-mqtt-broker`. Runtime/package-manager migration only:
keep Fastify, official MCP SDK, pg, Aedes, ws, mqtt, Zod, ESLint, Prettier,
tsc. No Bun.SQL / Bun.serve / Bun-WebSocket / framework rewrites this round.
No compatibility monkeypatches; a hard blocker is documented instead.

- [P1] Pin one exact stable Bun version (never `latest`/`canary`/`oven/bun:1`) and use the same version in both API projects, broker, Dockerfiles, docs, and `packageManager`.
- [P1] Baseline before any file changes: `npm run check` in restful-api + mcp-server-v2, `npm run check && npm test` in broker with the real isolated PostgreSQL setup when available; record results so migration failures are separable.
- [P1] Broker V8 persistence analysis: enumerate every table/column written via `packetBytes()` (expect `meshcore_private.retained_packets.packet`, `mqtt_outgoing.packet`, `mqtt_incoming.packet`, `mqtt_wills.packet`; grep to confirm) — Bun's `node:v8.serialize` is not wire-compatible with stored rows.
- [P1] Broker phase A (transition release, still Node): add portable versioned codec (`MESHMQTT1`-style magic prefix + @msgpack/msgpack payload) behind single `encodeStoredPacket`/`decodeStoredPacket` boundary; keep legacy V8 read path for old rows only; write new rows as MessagePack; preserve the existing strip-functions-before-persist rule; golden tests for binary fields (payload, correlationData, Uint8Array/Buffer normalization).
- [P1] Broker backfill script: idempotent, bounded batches, transactional, per-table report (legacy before / migrated / current after / failed), never rewrites current-format rows, verifiable afterwards. Run against production state and verify 0 legacy rows + restart recovery before any Bun broker rollout. Do not truncate persistent state.
- [P1] Broker phase B (after zero legacy rows): Jest → bun:test with semantic parity (fake timers → `setSystemTime`, restore after), test imports `../dist/*` → `../src/*`, remove jest/@jest-globals/jest.config/tsx/ts-node once parity proven; CLI entrypoint under Bun shebang; healthcheck `bun src/healthcheck.ts`; verify Aedes/ws/mqtt/pg/TLS/signals/recovery under real Bun runtime with test PostgreSQL.
- [P1] restful-api + mcp-server-v2: scripts on Bun (`dev` watch, `start` runs TS source directly, `test` = bun test), migrate Vitest imports to bun:test incl. REST's @typescript-eslint/rule-tester binding and MCP manifest determinism suite (23 tools, byte-identical schema fingerprint); remove vitest/tsx and dist build pipeline only if nothing external needs it; keep `tsc --noEmit` typecheck; add @types/bun, evaluate @types/node redundancy without losing strictness (strict, noUncheckedIndexedAccess, skipLibCheck).
- [P1] Lockfiles: create `bun.lock` per project from existing package-lock.json (verify content), then delete all three package-lock.json files; prove `rm -rf node_modules && bun install --frozen-lockfile` in each; ensure production containers install from lockfile with no auto-install/npm-registry access at runtime.
- [P1] Docker: replace node base images with pinned official slim Bun image per service; REST keeps ca-certificates/git/openssh-client; broker preserves root-prep/chown/setpriv privilege drop semantics and correct UID/GID; no healthcheck or entrypoint may require node/npm/npx; compose public ports/network contracts unchanged.
- [P2] Lifecycle/trust review: run `bun pm untrusted` per project; add explicit `trustedDependencies` only where genuinely needed; remove npm-only `allowScripts` if obsolete; decide fate of broker `scripts/check-lockfile-portability.mjs` (remove or minimal bun.lock text check, no custom parser).
- [P2] Metadata/docs sweep: `packageManager: bun@X.Y.Z`, drop stale `engines.node`, delete `.node-version`; update meshat-apis AGENTS.md (Bun is the permanent runtime/package manager, check commands become `bun run ...`), broker AGENTS.md ("one long-lived Bun broker process"), opencode.json permissions npm→bun, README/CONTRIBUTING/compose references, CI/Renovate configs; document broker rollback target = Node transition release (pure-V8 rollback unsafe after backfill).
- [P2] Verification gates: clean-install full checks in all three projects; broker real-PostgreSQL suite passes under Bun; graceful shutdown (SIGTERM/SIGINT) verified per service (sockets close, Fastify close completes, pool end, persistence flush, correct exit code); performance baseline before/after (image size, cold start, idle RSS, basic latencies) with regressions noted; final grep proving normal runtime/dev needs no node/npm/npx/tsx/vitest/jest remnants.
- [P2] Deploy sequence: REST+MCP after local verification (rollback = previous image); broker strictly staged — Node baseline → transition codec release → backfill → 0 legacy verified → restart recovery → Bun release → restart recovery + live MQTT smoke (connect/auth/allow+deny publish/subscribe/retained neighbors/WebSocket/QoS/wills/healthcheck). Live smokes: REST live-endpoints script and MCP live-manifest (`MCP_LIVE_BASE_URL=https://mcp.meshat.se bun run test:live`) must pass post-deploy with unchanged tool_schema_sha256.
- [P3] After stable Bun migration: evaluate pg → Bun.SQL separately (only if wanted).

## Carried-over open work

- [P3] Live-verified 2026-08-25: `/v1/docs/.git/*` returns an empty-body 403 from the host-level CrowdSec WAF (`remediationStatusCode: 403`) before reaching REST; the app itself already answers contract-compliant JSON 400 (verified via container-local request). Decide whether to tune the host Traefik/CrowdSec config so edge-blocked paths also carry an error body — outside workspace write scope, needs human/host action or explicit acceptance.
- [P2] Add a real PostgreSQL integration suite (`test:integration`) for query semantics the fake-based suite cannot prove: region membership via canonical evidence relation, entity-own region filtering, evidence-time correlation with `seen_from`/`seen_to`, cursor binding, logical-message canonical/matched split, representative packet linkage, trace observation identity, IATA aliases. The broker's `postgres/initdb` bootstrap is available in the read-only reference; use Testcontainers or an equivalent disposable PostgreSQL.
- [P2] Complete the single-source request-schema pattern to response schemas: `/v1/meshcore/nodes`, observers, packets, telemetry, traces, stats, activity still carry hand-written response JSON Schemas (they double as fast-json-stringify serialization whitelists). Migrating them requires guarding live-data variance with the PostgreSQL integration suite above; do not convert blindly.
- [P2] Global telemetry lacks an ideal leading timeline index in the public reference schema, so global telemetry keyset paging cannot be fully index-optimal until the broker project adds one.
- [P3] Stats uses exact full-table aggregate counts (~141 ms measured in production); replace with bounded/estimated counts if data growth makes this expensive.

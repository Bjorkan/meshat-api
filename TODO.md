# TODO

- [P3] Live-verified 2026-08-25: `/v1/docs/.git/*` returns an empty-body 403 from the host-level CrowdSec WAF (`remediationStatusCode: 403`) before reaching REST; the app itself already answers contract-compliant JSON 400 (verified via container-local request). Decide whether to tune the host Traefik/CrowdSec config so edge-blocked paths also carry an error body — outside workspace write scope, needs human/host action or explicit acceptance.
- [P2] Add a real PostgreSQL integration suite (`test:integration`) for query semantics the fake-based suite cannot prove: region membership via canonical evidence relation, entity-own region filtering, evidence-time correlation with `seen_from`/`seen_to`, cursor binding, logical-message canonical/matched split, representative packet linkage, trace observation identity, IATA aliases. The broker's `postgres/initdb` bootstrap is available in the read-only reference; use Testcontainers or an equivalent disposable PostgreSQL.
- [P2] Complete the single-source request-schema pattern to response schemas: `/v1/meshcore/nodes`, observers, packets, telemetry, traces, stats, activity still carry hand-written response JSON Schemas (they double as fast-json-stringify serialization whitelists). Migrating them requires guarding live-data variance with the PostgreSQL integration suite above; do not convert blindly.
- [P2] Global telemetry lacks an ideal leading timeline index in the public reference schema, so global telemetry keyset paging cannot be fully index-optimal until the broker project adds one.
- [P3] Stats uses exact full-table aggregate counts (~141 ms measured in production); replace with bounded/estimated counts if data growth makes this expensive.

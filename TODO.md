# TODO

- [P3] Live-verified 2026-08-25: `/v1/docs/.git/*` returns an empty-body 403 from the host-level CrowdSec WAF (`remediationStatusCode: 403`) before reaching REST; the app itself already answers contract-compliant JSON 400 (verified via container-local request). Decide whether to tune the host Traefik/CrowdSec config so edge-blocked paths also carry an error body — outside workspace write scope, needs human/host action or explicit acceptance.
- [P3] Cross-repo CI flake (2026-08-27, runs 33058053133 and 33081743610): "REST integration vs broker main" fails intermittently with transient 57P03 "the database system is starting up" immediately after the test container turns healthy; rerun passes unchanged on identical code. Add connect-retry/backoff for the first pool connection in restful-api/scripts/integration-provision.mjs.
- [P3] Post-Phase-8 re-baseline and messages optimization (2026-08-27): external HTTPS medians are not comparable to the old in-container snapshots; internal container-loopback baseline showed messages ~1.39 s warm (limit-independent), activity ~0.95–2.0 s, stats ~0.43–0.46 s against a ~0.007 s control floor — genuine DB cost, not transport. Root cause was full-set aggregation before LIMIT in listMessages; fixed by candidate-first pipeline (commit 773eebf). Post-deploy internal warm medians: messages limit=50 ~568 ms, limit=200 ~596 ms, iata=GOT ~882 ms (residual cost = qualifier DISTINCT pass), activity ~952 ms, stats ~457 ms. Next levers if volume grows again: qualifier semi-join index (broker-owned DDL, needs its own EXPLAIN evidence) and activity aggregation shape; no preaggregation until justified.

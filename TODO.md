# TODO

- [P3] Live-verified 2026-08-25: `/v1/docs/.git/*` returns an empty-body 403 from the host-level CrowdSec WAF (`remediationStatusCode: 403`) before reaching REST; the app itself already answers contract-compliant JSON 400 (verified via container-local request). Decide whether to tune the host Traefik/CrowdSec config so edge-blocked paths also carry an error body — outside workspace write scope, needs human/host action or explicit acceptance.
- [P3] Post index-drift-fix production medians (2026-08-26, in-container): stats 48 ms (exact counts dominate; do not approximate without a product decision), `/v1/meshcore/messages?limit=50` 156 ms (narrow-first pipeline; wide baseline ~73 ms at fixture scale), `/v1/meshcore/activity?window=24h&interval=1h` 208 ms — EXPLAIN shows the distinct logical-message aggregation over packet_observations dominates activity. Revisit only if volume grows; no preaggregation until justified.

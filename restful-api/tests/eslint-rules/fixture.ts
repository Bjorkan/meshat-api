/**
 * Virtual fixture target for the RuleTester suite in this directory.
 *
 * The type-aware rule tests parse ephemeral code snippets under this
 * module's path so that the project service treats them as part of the
 * tsconfig project. The file itself intentionally stays empty.
 */
export {};
